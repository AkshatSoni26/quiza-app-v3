import { createContext } from "react";

export const Helper = createContext()


export const messages = {
    right: 'Way to go! Your answer is a winner',
    wrong: 'Close, but not quite there yet. Keep pushing.',
};